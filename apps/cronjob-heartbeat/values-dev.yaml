# Unico contenido de este repositorio para "cronjob-heartbeat" en dev: el tag de imagen.
# gitops-update.yml (repo de codigo) actualiza esto via Pull Request tras
# build + Trivy + SBOM + cosign sign. El resto de la configuracion
# (recursos, replicas, probes) vive en el chart de Helm del repo de codigo
# (P8/helm/cronjob-heartbeat), no aqui.
image:
  tag: ""
